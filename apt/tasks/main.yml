---
# tasks file for apt
- name: Update apt
  ansible.builtin.apt:
    update_cache: true
    upgrade: true
- name: Install dependencies using apt
  ansible.builtin.apt:
    update_cache: true
    name: "{{ apt_packages }}"
    state: present
    autoremove: true
    autoclean: true
