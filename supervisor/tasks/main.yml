- name: Ensure supervisor is running
  ansible.builtin.systemd:
    name: supervisor
    state: started
    enabled: true

- name: Create Supervisor config for bench restart
  ansible.builtin.template:
    src: restart.conf.j2
    dest: /etc/supervisor/conf.d/restart.conf
    mode: "0644"
  notify: restart supervisor
  tags: supervisor
